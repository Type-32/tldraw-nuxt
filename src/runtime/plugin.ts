import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin((_nuxtApp) => {
  // Plugin for @type32/tldraw-nuxt
  // The TldrawNuxt component is automatically registered via the module
})
